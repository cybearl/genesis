import { LocalStorage } from "@sharedTypes/storage";


/**
 * The default local storage object.
 */
const defaultLocalStorage: LocalStorage = {
    lastSidebarState: "expanded",
    lastOpenedPage: 0
};

export default defaultLocalStorage;